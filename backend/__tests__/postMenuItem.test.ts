import * as postMenuItem from './../functions/postMenuItem/index';

describe(('takes a menu item and adds it to the database'), () => {

    test('if an item can be added', () => {

        
    });
});